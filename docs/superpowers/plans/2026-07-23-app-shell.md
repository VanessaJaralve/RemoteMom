# Working Mom App Shell Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first-session React Native app shell for Working Mom Command Center.

**Architecture:** Use Expo with React Native and React Navigation bottom tabs. Keep the color system in a shared constants file and create five focused placeholder screens matching the project brief.

**Tech Stack:** React Native, Expo, TypeScript, React Navigation, Jest, React Native Testing Library.

---

### Task 1: Project Setup And Failing Shell Tests

**Files:**
- Create: `package.json`
- Create: `app.json`
- Create: `tsconfig.json`
- Create: `babel.config.js`
- Create: `jest.config.js`
- Create: `jest.setup.js`
- Create: `__tests__/App.test.tsx`

- [x] **Step 1: Add project configuration**

Create the Expo/TypeScript/Jest configuration needed to install dependencies and run tests.

- [x] **Step 2: Write failing tests**

Test that all five tab labels render, placeholder screen content exists, and the brief's life-area colors are exported.

- [x] **Step 3: Run test to verify it fails**

Run: `pnpm test -- --runInBand`
Expected: fail because `App` and shared colors do not exist yet.

### Task 2: App Shell Implementation

**Files:**
- Create: `App.tsx`
- Create: `src/constants/colors.ts`
- Create: `src/screens/TodayScreen.tsx`
- Create: `src/screens/HealthScreen.tsx`
- Create: `src/screens/KidScreen.tsx`
- Create: `src/screens/GroceryScreen.tsx`
- Create: `src/screens/TodosScreen.tsx`

- [x] **Step 1: Implement shared color constants**

Export exact hex values for Work, Kid, Health, and Household.

- [x] **Step 2: Implement placeholder screens**

Each screen renders a title, short placeholder message, and relevant color tag.

- [x] **Step 3: Implement bottom tab navigation**

Use `createBottomTabNavigator` with the exact tab labels: Today, Health, Kid, Grocery, To-Dos.

- [x] **Step 4: Run test to verify it passes**

Run: `pnpm test -- --runInBand`
Expected: all tests pass.

### Task 3: Local Run Verification

**Files:**
- Modify: generated lockfile after dependency install

- [x] **Step 1: Install dependencies**

Run: `pnpm install`
Expected: dependencies resolve and lockfile is created.

- [x] **Step 2: Start the dev server**

Run: `pnpm start -- --host localhost`
Expected: Expo dev server starts and prints a local URL.

