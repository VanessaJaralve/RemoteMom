import React from 'react';

import { ScreenPlaceholder } from './ScreenPlaceholder';

export function TodayScreen() {
  return (
    <ScreenPlaceholder
      description="Placeholder timeline for daily priorities."
      lifeArea="work"
      tagLabel="Work"
      title="Today Dashboard"
    />
  );
}
